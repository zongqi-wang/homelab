
# 1. Install Tailscale

```bash
curl -fsSL https://tailscale.com/install.sh | sh
```

```bash
sudo tailscale up
```


```bash
sudo tailscale set --ssh
```



# 2. Install 3x-ui
```bash
curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh | sudo bash
```


```
Check panel output for credentials after install.
```