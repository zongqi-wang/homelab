# gitlab environment variables
# Copy this to .env and fill in real values before deploying.

# GitLab generates its own root password on first init.
# Set this to override the initial root password:
# GITLAB_ROOT_PASSWORD=changeme
